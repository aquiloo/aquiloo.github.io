---
interface Props {
  title: string;
  description: string;
}

const { title, description } = Astro.props;
---

<section class="site-shell page-intro">
  <p class="eyebrow">Archive</p>
  <h1>{title}</h1>
  <p>{description}</p>
</section>
